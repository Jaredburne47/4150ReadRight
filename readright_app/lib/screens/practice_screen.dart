import 'package:flutter/material.dart';

//Practice screen for student

class PracticeScreen extends StatelessWidget {
  const PracticeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Practice Screen (Microphone and pronunciation UI here)', style: TextStyle(fontSize: 18)),
    );
  }
}
